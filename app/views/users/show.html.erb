<h1><%= link_to "Good Reels", "/" %></h1>
<h2><%= @user.name %></h2>
<p><%= @user.email %></p>
<%= will_paginate @movies %>
<div>
    <% if logged_in? %>
        <% if @user.movies.any? %>
          <h3>Movies (<%= @user.movies.count %>)</h3>
          <ol class="movies">
            <%= render @movies %>
          </ol>
          <%= will_paginate @movies %>
        <% end %>
    <% end %>
</div>
<%= will_paginate @movies%>